# Storage

Responsavel por repositorios, conexoes, migracoes, modelos persistidos, adaptadores de banco e politicas de backup/restore.

O `banco.py` atual pertence ao Promogg e nao deve ser movido sem testes de equivalencia e plano de migracao.

